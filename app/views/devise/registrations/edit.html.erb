<div class="max-w-lg mx-auto">
  <div class="mb-8">
    <h2 class="text-2xl font-bold text-gray-900">Edit Profile</h2>
    <p class="text-gray-500 text-sm mt-1">Update your account details</p>
  </div>

  <div class="bg-white rounded-2xl shadow-sm border border-gray-200 p-8">
    <%= form_for(resource, as: resource_name,
          url: registration_path(resource_name),
          html: { method: :put }) do |f| %>

      <%# Error messages %>
      <% if resource.errors.any? %>
        <div class="mb-6 bg-red-50 border border-red-200 rounded-lg p-4">
          <ul class="list-disc list-inside text-red-600 text-sm space-y-1">
            <% resource.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div class="grid grid-cols-2 gap-4 mb-5">
        <div>
          <%= f.label :first_name, class: "block text-sm font-medium text-gray-700 mb-1" %>
          <%= f.text_field :first_name,
                class: "w-full px-4 py-2.5 border border-gray-300 rounded-lg
                        focus:outline-none focus:ring-2 focus:ring-blue-500
                        focus:border-transparent text-gray-900 text-sm" %>
        </div>
        <div>
          <%= f.label :last_name, class: "block text-sm font-medium text-gray-700 mb-1" %>
          <%= f.text_field :last_name,
                class: "w-full px-4 py-2.5 border border-gray-300 rounded-lg
                        focus:outline-none focus:ring-2 focus:ring-blue-500
                        focus:border-transparent text-gray-900 text-sm" %>
        </div>
      </div>

      <div class="mb-5">
        <%= f.label :email, class: "block text-sm font-medium text-gray-700 mb-1" %>
        <%= f.email_field :email,
              class: "w-full px-4 py-2.5 border border-gray-300 rounded-lg
                      focus:outline-none focus:ring-2 focus:ring-blue-500
                      focus:border-transparent text-gray-900 text-sm" %>
      </div>

      <div class="mb-5">
        <%= f.label :phone_number, class: "block text-sm font-medium text-gray-700 mb-1" %>
        <%= f.telephone_field :phone_number,
              class: "w-full px-4 py-2.5 border border-gray-300 rounded-lg
                      focus:outline-none focus:ring-2 focus:ring-blue-500
                      focus:border-transparent text-gray-900 text-sm" %>
      </div>

      <div class="border-t border-gray-100 pt-5 mb-5">
        <p class="text-sm font-medium text-gray-700 mb-4">
          Change Password <span class="text-gray-400 font-normal">(leave blank to keep current)</span>
        </p>
        <div class="mb-4">
          <%= f.label :password, "New Password",
                class: "block text-sm font-medium text-gray-700 mb-1" %>
          <%= f.password_field :password,
                autocomplete: "new-password",
                class: "w-full px-4 py-2.5 border border-gray-300 rounded-lg
                        focus:outline-none focus:ring-2 focus:ring-blue-500
                        focus:border-transparent text-gray-900 text-sm" %>
        </div>
        <div class="mb-5">
          <%= f.label :password_confirmation, "Confirm New Password",
                class: "block text-sm font-medium text-gray-700 mb-1" %>
          <%= f.password_field :password_confirmation,
                autocomplete: "new-password",
                class: "w-full px-4 py-2.5 border border-gray-300 rounded-lg
                        focus:outline-none focus:ring-2 focus:ring-blue-500
                        focus:border-transparent text-gray-900 text-sm" %>
        </div>
      </div>

      <div class="mb-6">
        <%= f.label :current_password, class: "block text-sm font-medium text-gray-700 mb-1" %>
        <%= f.password_field :current_password,
              autocomplete: "current-password",
              class: "w-full px-4 py-2.5 border border-gray-300 rounded-lg
                      focus:outline-none focus:ring-2 focus:ring-blue-500
                      focus:border-transparent text-gray-900 text-sm" %>
        <p class="mt-1 text-xs text-gray-400">Required to save any changes</p>
      </div>

      <%= f.submit "Save Changes",
            class: "w-full bg-blue-600 hover:bg-blue-700 text-white font-semibold
                    py-2.5 px-4 rounded-lg transition cursor-pointer text-sm" %>

    <% end %>
  </div>
</div>